# kernlab SVM

    Code
      characterize(svm_mod)
    Output
      # A tibble: 1 x 3
        .metric             .estimator .estimate
        <chr>               <chr>          <dbl>
      1 num_support_vectors model             42

